#include <iostream>
#include <string>

#define DEFAULT_SIZE 100

using namespace std;

int count_nonzero_elements(int array[DEFAULT_SIZE], int size);
void print(int array[DEFAULT_SIZE], int size, string name, int expected);
string convert(int array[DEFAULT_SIZE], int size);
